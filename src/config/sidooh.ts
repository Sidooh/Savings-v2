export default {
    services: {
        notify: {
            url: process.env.SIDOOH_NOTIFY_API_URL
        },
        accounts: {
            url: process.env.SIDOOH_ACCOUNTS_API_URL
        }
    }
}